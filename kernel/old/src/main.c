

void kernel_main(void){
    //asm volatile ("1: jmp 1b");
    //asm volatile("int 3");
    for (int i = 0; i < 10000000; i++);
    
    //int nul = 0 / 0;
    //asm volatile ("1: jmp 1b");

    while(1);
}